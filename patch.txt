diff --git a/actions.py b/actions.py
index e201c4e..9022116 100644
--- a/actions.py
+++ b/actions.py
@@ -23,13 +23,20 @@ env_vars = {
 def handle_read(command):
     parts = command.split() # 按空格拆成多个string的list ["xxx", "aaaa"]
     filename= parts[1]
-    line_range = parts[2]
 
-    with open(filename,"r") as f:
-        lines = f.readlines() # 返回一个列表，索引对应不同行
+    if len(parts) == 4:
+        # read file.py 10 20 空格的情况
+        start, end = int(parts[2]), int(parts[3])
+    elif "-" in parts[2]:
+        # read file.py 10-20 
+        start,end = parts[2].split("-")
+        start,end = int(start),int(end)
+    else:
+        return "请重新给出合理的bash指令。 目前无法匹配"
 
-    start,end = line_range.split("-")
-    start,end = int(start),int(end)
+    with open(filename,"r") as f:
+        lines = f.readlines() # 返回一个列表，索引对应不同行    
+    
     result = lines[start-1:end]
     return "".join(result)
 
@@ -41,11 +48,16 @@ def handle_write(command):
     specific_line = parts[2]
     new_content = parts[3]
 
+    if not os.path.exists(filename):
+
+        with open(filename,"w") as f: #纯没有文件
+            f.write(new_content + "\n")
+        return "新文件已创建"
+
     with open(filename,"r") as f:
         lines = f.readlines()
 
     lines[int(specific_line)-1] =  new_content + "\n" #将读出来的lines 然后根据命令的行数修改
-
     with open(filename, "w") as f:
         f.writelines(lines)
     return "已修改成功"
diff --git a/system_prompt.txt b/system_prompt.txt
index e269a42..71470d2 100644
--- a/system_prompt.txt
+++ b/system_prompt.txt
@@ -1,10 +1,17 @@
-你是一个代码编写的agent, 你需要根据用户的初步指令去是否选择计划而去完成目标， 
-并且根据你代码出现的错误进行自我修正知道完成用户目标。如果你需要跑一个指令， 
-请用以下两种方式包装（一次回答只给一个！）：
-1. 你优先给我使用 "read file.py 10-20", "write file.py 15 new_content", "search keyword ./dir"来操作文件
-2. 可以输出
- ```bash-action\n<command>\n```.  
-如果你认为任务已经完成，请运行exit-verified 指令（不要陷入死循环）。  目前agent的步骤分为三个：1. listening stage 2. plan stage（可选） 3. Execution and correction stage   
-Plan的时候只说计划，不给代码。 Plan的时候用户说ok，说确认才给finished.， 没说的时候不可以给，不可以给计划的时候同时给finish。 
-另外，在修bug类任务时，先写一个最小复现脚本确认bug存在，改完代码后重跑复现脚本确认修好了，通过了才能exit-verified。
-改完代码后，必须找到仓库里覆盖你改动模块的已有测试文件跑一遍。定位到根因后grep同文件里有没有同样的错误格式，一并修掉。
\ No newline at end of file
+你是一个代码编写的agent, 请根据用户指令完成任务，遇到错误自我修正
+严肃按照我一下的声明进行任务。
+
+【输出格式】
+所有命令用```bash-action\n<command>\n``` 包装，一次只给一个。
+文件操作优先用: read file.py 10-20 / write file.py 15 new_content / search keyword ./dir
+
+【任务完成】
+修bug 时：1. 先写复现脚本确认bug存在 2. 改代码 3. 重跑复现脚本 4. 跑仓库已有测试 5. grep同文件同类错误一并修掉 6. exit-verified
+非bug任务： 1. 完成后验证结果正确 2. exit-verified。
+
+【Planning】
+需要plan时先只说计划不给代码。用户说ok/确认后输出 plan-finish (单独一行，不带代码)。
+
+【Sub-Agent】
+复杂任务可以 spawn 子agent : echo "任务描述" > task.txt && python My_Agent.py task.txt 子agent 独立运行，结果在patch.txt
+注意：不需要先读项目文件再 spawn, 直接写任务文件并执行即可。
\ No newline at end of file
diff --git a/weather.py b/weather.py
deleted file mode 100644
index 6814118..0000000
--- a/weather.py
+++ /dev/null
@@ -1,85 +0,0 @@
-import sys
-import os
-import urllib.request
-import urllib.parse
-import json
-
-
-def get_weather(city: str):
-    # 使用 Open-Meteo 免费 API，无需 key
-    # 先用 geocoding API 把城市名转为经纬度
-    geo_url = "https://geocoding-api.open-meteo.com/v1/search?" + urllib.parse.urlencode({
-        "name": city,
-        "count": 1,
-        "language": "zh",
-        "format": "json",
-    })
-    with urllib.request.urlopen(geo_url, timeout=10) as resp:
-        geo_data = json.loads(resp.read().decode("utf-8"))
-
-    if "results" not in geo_data or not geo_data["results"]:
-        return None, f"找不到城市: {city}"
-
-    loc = geo_data["results"][0]
-    lat, lon = loc["latitude"], loc["longitude"]
-    name = loc.get("name", city)
-    country = loc.get("country", "")
-
-    weather_url = "https://api.open-meteo.com/v1/forecast?" + urllib.parse.urlencode({
-        "latitude": lat,
-        "longitude": lon,
-        "current": "temperature_2m,relative_humidity_2m,wind_speed_10m,weather_code",
-        "timezone": "auto",
-    })
-    with urllib.request.urlopen(weather_url, timeout=10) as resp:
-        w = json.loads(resp.read().decode("utf-8"))
-
-    cur = w.get("current", {})
-    return {
-        "location": f"{name}, {country}".strip(", "),
-        "temperature": cur.get("temperature_2m"),
-        "humidity": cur.get("relative_humidity_2m"),
-        "wind": cur.get("wind_speed_10m"),
-        "code": cur.get("weather_code"),
-        "time": cur.get("time"),
-    }, None
-
-
-WEATHER_CODES = {
-    0: "晴", 1: "多云", 2: "多云", 3: "阴",
-    45: "雾", 48: "雾凇",
-    51: "小毛毛雨", 53: "毛毛雨", 55: "大毛毛雨",
-    61: "小雨", 63: "中雨", 65: "大雨",
-    71: "小雪", 73: "中雪", 75: "大雪",
-    80: "小阵雨", 81: "阵雨", 82: "强阵雨",
-    95: "雷暴", 96: "雷暴伴冰雹", 99: "强雷暴伴冰雹",
-}
-
-
-def format_weather(info):
-    code = info.get("code")
-    desc = WEATHER_CODES.get(code, f"未知({code})")
-    return (
-        f"📍 {info['location']}\n"
-        f"🕐 时间: {info['time']}\n"
-        f"🌡️ 温度: {info['temperature']}°C\n"
-        f"💧 湿度: {info['humidity']}%\n"
-        f"💨 风速: {info['wind']} km/h\n"
-        f"☁️ 天气: {desc}\n"
-    )
-
-
-def main():
-    if len(sys.argv) < 2:
-        print("用法: python weather.py <城市名>")
-        sys.exit(1)
-    city = " ".join(sys.argv[1:])
-    info, err = get_weather(city)
-    if err:
-        print(f"错误: {err}")
-        sys.exit(2)
-    print(format_weather(info))
-
-
-if __name__ == "__main__":
-    main()
